spring:
  application:
    name: radiology-registry
  datasource:
    url: jdbc:postgresql://localhost:5432/radiology_registry
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

server:
  port: 8080